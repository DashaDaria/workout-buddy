<h1>Edit Workout</h1>
<%=form_for(@workout, url: workout_path(@workout)) do |f|%>
<p><%=f.text_field :name, placeholder: "Name your workout"%></p>
Completed? <%=f.check_box :completed%>

<p>Choose an existing exercise or create your own:</p>

<p><%= f.collection_check_boxes(:exercise_ids, Exercise.all, :id, :name)%></p>


<%= f.submit %>
<%end%>
